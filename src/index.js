import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

//Quero renderizar algo (componente), em determinado local (root)
ReactDOM.render(<App />, document.getElementById('root'));
